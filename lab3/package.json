{
  "name": "notes-app",
  "version": "1.0.0",
  "description": "",
  "main": "notes-app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel notes-app/js/index.js --watch --out-file test/js/index.js --presets=env -s"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
